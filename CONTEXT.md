# 术语表

文档和代码里这些词的含义，以此为准。写文档、命名变量、提需求时都用这里的说法，避免同一个东西出现好几个名字。

**角色**
被对话的一方，由角色名和角色描述（system prompt）构成。语音和 Live2D 是它的可选表现能力，不是它的身份。
_不叫_：AI、机器人、模型

**会话**
一段连续的对话，由 `session_id` 标识，包含若干回合。M1 只存在内存里，关掉程序就没了。
_不叫_：聊天记录、上下文

**回合**
用户发一条消息到角色回复完（含语音播完）的完整过程，由 `turn_id` 标识。一次回合内可能有多个音频分片。
_不叫_：一次请求、一轮对话

**事件**
模块之间传递的统一消息对象，有 `type` 和 `turn_id`。跨进程、跨界面都只用它，不许自己造别的格式。
_不叫_：消息、信号、回调

**分片**
一段可以独立播放的音频，对应回复里的一句话。上层按标点切句，每句一个分片。
_不叫_：音频块、chunk、segment

**音色档案**
描述"用什么音色说话"的配置对象，由后端名和该后端的专属参数组成。edge 后端的参数是音色 ID 和语速；将来本地模型的参数是模型路径、参考音频、参考文本、语言。
_不叫_：voice、音色配置、TTS 参数

**合成中**
TTS 的状态之一。界面从第一天就要能显示它，不能假设语音是瞬间出来的——将来换本地模型后，第一次出声可能要等几十秒。
_不叫_：loading

**打断**
取消当前回合：用户又发了一条消息，或手动停止。之后属于这个回合的事件一律丢弃，播放立刻停止，口型归位。
_不叫_：取消请求、abort、中断

**口型驱动**
用正在播放的音频的音量（RMS）去驱动嘴巴张合参数。**不使用** edge-tts 的逐字时间戳，因为本地模型没有这个能力，现在用了将来要重写。
_不叫_：唇形同步、lip sync、音素对齐

**动作组**
Live2D 模型里的一组命名动作，比如 `idle`、`happiness`、`talk`。映射表输出的是动作组名，不是 live2d-py 的参数值。
_不叫_：motion、动画

**表情**
叠加在动作之上的面部参数组合，比如 `smile`、`sad`。和动作组一样，映射表只输出名字。
_不叫_：expression 文件、脸部状态

**核心层**
不依赖任何界面的那一层：`runtime.py`、`llm/`、`tts/base.py`、`emotion/`。它不许 import PyQt5、pygame、edge_tts。这样单测能在 CI 里跑，将来加 Web 接口也不用重写。
_不叫_：业务层、后端、service

**渲染进程**
单独跑 Live2D 的子进程（pygame + OpenGL + live2d-py）。它同时负责播放音频——因为口型要用正在播的那段音频的音量，同进程最省事。
_不叫_：Live2D 窗口、viewer、渲染线程

**一键启动**
本机脚本启动（Windows 下 `run.bat`）。**不是** Docker 容器——这个项目不用 Docker，理由见 ADR 0002。
_不叫_：docker up、部署脚本

**故意不做**
明确决定不做的功能，不是漏掉的。清单在 `docs/product.md` 里，实现时不要顺手补上。
_不叫_：TODO、以后再说、待办
